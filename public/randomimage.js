window.addEventListener("load", (event) => {
    var randomImage = new Array("assets/multimedia/images/rand/gmaine.png");
    var imageNum = Math.floor(Math.random()*randomImage.length);
    document.getElementById("gmane").src = (piss[random404]);
});